A coworker has invited you to share your salary with the following group:

<%= @group.name %>

<%= @group.invitations_count %> of your coworkers have already been invited.

Accept this invitation here <<%= join_group_url(@group) %>>.

PaySymmetry is a site that allows employees to anonymously share their
salaries. Yadada it's awesome...
